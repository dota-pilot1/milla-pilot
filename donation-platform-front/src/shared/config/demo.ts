/**
 * 시연용 노출 스위치.
 *
 * <p>파일럿 단계에서 배포 사이트로 기능을 보여주려면 시드 계정 선택기가 필요하다.
 * 다만 선택기는 <b>관리자 계정의 이메일과 공용 비밀번호를 화면에 그대로 노출</b>한다.
 * 즉 이 값이 true 인 동안에는 누구나 플랫폼 관리자로 로그인할 수 있다.
 *
 * <p><b>실제 후원자가 한 명이라도 가입하면 즉시 false 로 바꾼다.</b> 관리자로 들어가면
 * 후원 원장에서 타인의 이름·전화번호·후원 내역을 볼 수 있어, 그때부터는 우리 손해가
 * 아니라 그 사람의 개인정보 문제가 된다.
 *
 * <p>환경변수가 아니라 상수인 이유 — 정적 export 라 빌드 시점에 값이 박히고,
 * 배포 파이프라인에 변수를 새로 심는 것보다 이 한 줄을 고치는 편이 확실하다.
 */
export const SHOW_SEED_ACCOUNTS = true;
